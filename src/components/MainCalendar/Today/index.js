import React from 'react';
import styles from "./Today.module.scss";
const Today = () => {
    return (
        <div className={styles.calendarConainer}>
            <p className={styles.today}>12</p>
        </div>
    );
}

export default Today;
